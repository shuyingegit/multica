import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "新建工作区",
};

export default function NewWorkspaceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
