import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "关注的票",
};

export default function MobilePinsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
